# สัญญาข้อมูลสำหรับผู้ช่วย AI

เอกสารนี้เขียนให้ **ผู้ช่วยตัวอื่นที่แตะข้อมูล TickTick ชุดเดียวกัน** (เช่น skill
`klui-ai-secretary` บน claude.ai) ทำตามนี้แล้วแดชบอร์ดกับห้องแชทจะเห็นภาพตรงกัน

แดชบอร์ดในรีโปนี้ **ไม่มีฐานข้อมูลของตัวเอง** ทุกอย่างอ่านจาก TickTick ผ่าน Apps Script
ดังนั้นแท็กใน TickTick คือสัญญาเดียวที่ผูกสองระบบเข้าด้วยกัน ตั้งแท็กผิดรูปแบบ = แดชบอร์ดมองไม่เห็น

## แท็กสามมิติ

| รูปแบบ | ความหมาย | ค่าที่ใช้อยู่จริง |
|---|---|---|
| `⚡<ระดับ>` | พลังงานที่ต้องใช้ | `⚡สูง` `⚡กลาง` `⚡ต่ำ` |
| `📍<ที่>` | ต้องอยู่ที่ไหนถึงทำได้ | `📍หน้าคอม` `📍บ้าน` `📍ศิริราช` `📍ที่ไหนก็ได้` |
| `⏱<นาที>` | เวลาที่คาดว่าจะใช้ | `⏱15` `⏱30` `⏱60` `⏱90` `⏱120` |

กติกาของตัวแยกแท็ก (`parseTag()` ใน `index.html`):

- **`⚡` รับเฉพาะคำที่รู้จัก** — สูง / กลาง / ปานกลาง / ต่ำ / high / mid / medium / low
  คำอื่นถูกทิ้งเงียบ ๆ เช่น `⚡มั่ว` ไม่ถูกอ่านเป็นพลังงาน
- **`📍` รับข้อความอะไรก็ได้** ที่ตามหลัง เว้นวรรคในชื่อได้ (`📍ห้องยา ชั้น 2`)
  สถานที่ใหม่โผล่ในตัวกรองเองหลังซิงก์ ไม่ต้องแก้โค้ด — แต่ **ตั้งชื่อให้ตรงกับที่มีอยู่**
  ไม่งั้นจะกลายเป็นสถานที่คนละอันและจัดกลุ่มแตก
- **`⏱` รับจำนวนเต็มอะไรก็ได้** (`⏱25` `⏱76` ใช้ได้) แต่ควรอยู่ในชุด 5 ค่าข้างบน
  เพื่อไม่ให้รายการแท็กใน TickTick บานปลาย
- **variation selector (U+FE0F) ถูกตัดก่อนเทียบ** `⏱️30` กับ `⏱30` จึงเป็นแท็กเดียวกัน
  พิมพ์แบบไหนก็ได้

## แท็กเดิมที่ถูกตีความให้อัตโนมัติ

| แท็ก | ตีเป็น |
|---|---|
| `🎯 ต้องต่อเนื่อง` | พลังงานสูง |
| `🔄 แทรกได้` | พลังงานต่ำ |

**แท็ก `⚡` ที่ติดเองชนะเสมอ** ไม่ว่าจะมาก่อนหรือหลังในลิสต์ งานหลายอันจึงมีทั้ง
`🔄 แทรกได้` และ `⚡สูง` พร้อมกันได้ — ระบบใช้ `⚡สูง` ไม่ต้องไปลบแท็กเดิมทิ้ง

## เวลาที่ถูกจองแล้ว vs งานที่ต้องทำ

แดชบอร์ดแยกสองอย่างนี้ด้วยฟิลด์ `entityClass` ที่ backend ใส่มาให้ (`quest` / `event`)

- **event** = เวรและนัด กินเวลาแต่ทำเสร็จไม่ได้ ไม่ได้ XP ไม่ต้องติดแท็กสามมิติ
- **quest** = งานที่ต้องลงมือ ควรมีแท็กครบสามมิติ

หน้า "วันนี้" และ "จัดกลุ่ม" เอาเวลาของ event มาหักออกก่อน แล้วค่อยเทียบกับผลรวม `⏱`
ของงาน เพื่อตอบว่าวันนั้นวางแผนไว้เกินตัวหรือยัง กติกาการนับ:

- เวรข้ามเที่ยงคืน**ตัดแบ่งตามวันไทย** — `16:30–00:00` กินสองวัน, `2:30–8:30` อยู่วันเดียว
- event ที่ `startDate` เท่ากับ `dueDate` = **ไม่รู้ว่ากินเวลาเท่าไร** นับหัวไว้เฉย ๆ ไม่เดาชั่วโมง
  (เช่น "คลินิกพิเศษ — โปรดตรวจสอบจากตารางเวรอีกครั้ง")
- เวลาว่างประมาณจากวันตื่น 16 ชม. เป็นค่าคงที่ ไม่ใช่ตารางเวลาจริง

**ที่ต้องระวัง:** งานประจำ จ.–ศ. 08:30–16:30 ที่ศิริราชไม่ได้อยู่ในปฏิทิน
วันธรรมดาที่ดูว่างในงบเวลาจึงไม่ได้ว่างจริง

## เขียนแท็กผ่าน API

`batch_update_tasks` **แทนที่อาร์เรย์ `tags` ทั้งก้อน** ไม่ใช่เพิ่มเข้าไป
ต้องอ่านแท็กเดิมมาใส่กลับไปด้วยทุกครั้ง ไม่งั้นแท็กโปรเจกต์ (`ตารางเวรเภสัช`,
`การเงิน`, `sync`, `gcal`) จะหายหมด

ส่ง `id` กับ `projectId` พอ — ฟิลด์อื่นที่ไม่ส่งไม่ถูกล้าง (ทดสอบแล้วกับ
`title` `dueDate` `priority` `sortOrder` `items`) แต่ **ยืนยันด้วย `get_task_by_id`
หนึ่งรายการก่อนยิงเป็นก้อนเสมอ**

`projectId` ของ Inbox ที่ API ต้องการคือ `inbox125681541` ไม่ใช่ `inbox`

## สถานะปัจจุบัน

ติดแท็กครบสามมิติแล้ว 27 งาน — Inbox 15 งาน และโปรเจกต์ `Google calendar` 12 งาน

ยังไม่ได้ติด: `บอทรับเวรอัตโนมัติจากกลุ่ม LINE` (พักโปรเจกต์ไว้ ถ้าติดแท็กจะไปดัน
เวลารวมในก้อนงานทั้งที่ยังไม่ได้ทำ) — ปลดพักเมื่อไรค่อยติด

ค่า `⏱` ของคลิป YouTube เป็นการเดาจากชื่อเรื่อง ยังไม่ได้เทียบกับความยาวจริง

## ถ้าจะแก้ตัวแยกแท็ก

`parseTag()`, `buildIndexes()`, `dayBudget()` ใน `index.html` เป็นจุดเดียวที่ตีความแท็ก
ทุกมุมมองอ่านต่อจากตรงนั้น แก้ที่นี่ที่เดียวพอ
